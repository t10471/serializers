namespace P2P;

enum MessageType : byte { Handshake = 0, SendTransaction, SendBlock, Bye }
enum Status : byte { OK = 0, NG }

table Message {
    type:MessageType;
    status:Status;
    payload:[byte];
}

root_type Message;